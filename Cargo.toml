[package]
name = "elatum-kb-optim"
version = "0.0.0"
edition = "2021"

[profile.release]
codegen-units = 1
lto = "thin"
# lto = "fat"
opt-level = 3
panic = "abort"
strip = true
#target-cpu = "native" -> RUSTFLAGS='-C target-cpu=native'

[dependencies]
nalgebra = "0.32.3"
rand = "0.8.5"
